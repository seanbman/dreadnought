[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dreadnought-control-plane"
version = "0.1.0"
description = "Experimental agent control plane with normalized missions and auditable evidence."
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "grapher @ git+https://github.com/seanbman/grapher.git@afba1021f3d1d876b7a30cc7b1e9f66b1fa0efe3"
]

[project.scripts]
dreadnought = "dreadnought.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
